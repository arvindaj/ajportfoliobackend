services:
  - type: web
    name: my-web-service
    env: node
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    plan: free
    branch: main
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
